groups:
  - name: test
    rules:
      - alert: TestAlert
        expr: vector(1)
        for: 30s
        labels:
          severity: warning
        annotations:
          summary: "🔥 Test Alert"
          description: "This is a test alert from Prometheus"
